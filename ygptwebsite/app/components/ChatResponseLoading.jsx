const ChatResponseLoading = () => {
  return (
    <div className="p-3" style={{ paddingInline: `calc(50vw - var(--container-width) / 2)` }}>
      <div class="loader"></div>
    </div>
  );
};

export default ChatResponseLoading;
